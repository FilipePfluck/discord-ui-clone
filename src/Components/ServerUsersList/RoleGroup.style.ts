import {styled} from '../../../stitches.config';

export const Container = styled('div', {
	margin: '16px 0',

	'>p': {
		fontSize: 12,
		color: '$unimportant',
		fontWeight: 600,
		marginLeft: 8,
	},
});
